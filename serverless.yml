service: copa-services

provider:
  name: aws
  runtime: nodejs6.10

functions:
  echo:
    cljs: copa-services.core/echo
    events:
      - http:
          path: echo
          method: post

plugins:
  - serverless-cljs-plugin
